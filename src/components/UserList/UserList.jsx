import React from 'react'

const UserList = () => {
  return (
    <div>
      {/* {data && data.length > 0 && data.map((item) => <p>{item.name}</p>)} */}
      hi
    </div>
  )
}

export default UserList
